include <iostream>

int main()
{
    int openwindows =1;
    std::string localname = "local";
    int  i = 1;
    while (i == 1) {
        std::cout << localname << ":/";
        std::string input;
        std::cin >> input;
        if (input == "help") {
            std::cout << "print prints line, opn opens a new window, ammountopen tells how many windows are open,local name changes the name for the local keyword, r print is wyuetruwebrtywevbquyrftvgqweuyr";
        }
        else if (input == "print" ) {
            std::cout << "enter what to print:";
            std::string printput; 
            std::cin >> printput;
            std::cout << printput;
            printput == "";
        }
        
        else if (input == "opn") {
            main();
            openwindows++;
        }
        else if (input == "ammountopen") {
            std::cout << openwindows;
        }
        else if (input == "rprint") {
            while (i == 1) {
                std::cout << "CrAShINg";
            }
        }
        else if (input == "localname") {
            std::cout << "enter new local name";
            std::cin >> localname;
            
        }
        
